// range
package main

import "fmt"

func main() {
	mumeros := []int{2,4,6}
	suma:=0
	for _, num:= range numeros{
		suma += numeros
	}
	fmt.Println("suma:" suma)

	for i, numero:= range numeros{
		if numero== 3
		fmt.Println("index:", i)
	}
}

// iterar sobre el mapa usando range e imprimir

algo := map[string]string{"a":auto, "b":bebe}

for i := 0; i < count; i++ {
	
}